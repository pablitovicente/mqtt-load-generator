---
# Example Secret for the MQTT password used by job.yaml and checker-deployment.yaml.
# Change the value below before applying this to a real cluster.
apiVersion: v1
kind: Secret
metadata:
  name: mqtt-load-generator
type: Opaque
stringData:
  password: mega_secret # CHANGE ME
